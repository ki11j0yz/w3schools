# the clear() method empties the list - but the list still remains, just without content
mylist = ["apple", "banana", "cherry"]
mylist.clear()
print(mylist)   # **will output '[]' to signify an empty list