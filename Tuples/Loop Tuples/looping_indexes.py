# NOTE: You can loop through tuple items by referring to their index #
# NOTE: Use range() and len() functions to create sutiable iterable

rocks = ("galena", "lead", "pyrite", "quartz", "sandstone")
for i in range(len(rocks)):
    print(rocks[i])
    